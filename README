ANT -- Algebraic Number Theory Library
======================================

ANT is an algebraic number theory extension module for flint2 (see http://flintlib.org).

To install it in flint, clone the repository into a directory on your filesystem, e.g. /home/username/ant

Now build flint in the usual way, passing --extensions=/home/username/ant to flint's configure.

William Hart -- 12 May 2013.
